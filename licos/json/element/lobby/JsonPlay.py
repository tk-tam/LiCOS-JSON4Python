import licos.json.element.lobby
import jsons
from typing import List
from dataclasses import dataclass


@dataclass
class Jsonplay(
        type: str,
        token: str,
        villageId: long
        TypeSystem(type)):

    def _validType -> str:
        return JsonPlay.type


class JsonPlay:

    type: str = "play"
