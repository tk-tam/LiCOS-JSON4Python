import licos.json.element.village.role

import licos.json.element.village.JsonElement
import licos.json.element.village.JsonName


class JsonAbstractRole(
    @context: str,
    @id: str,
    name: JsonName,
    image: str,
    JsonElement)
