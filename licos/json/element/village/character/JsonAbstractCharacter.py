import licos.json.element.village.character

import licos.json.element.village.JsonElement
import licos.json.element.village.JsonName

from typing import List

class JsonAbstractCharacter(
    @context: str,
    @id: str,
    id: long,
    name: JsonName,
    image: str,
    JsonElement)
