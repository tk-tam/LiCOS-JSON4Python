import licos.json.element.village

import licos.json.element.village.iri.AvatarContext

from typing import List
from dataclasses import dataclass

@dataclass
class JsonAvatar(
    @context: str,
    @id: str,
    token: str,
    name: str,
    image: str,
    JsonElement):

    def __init__(self,self.AvatarContext.iri: str = AvatarContext.iri,　id: str,
        token: str,
        name: str,
        image: str,
    ):
    pass


class JsonAvatar:
